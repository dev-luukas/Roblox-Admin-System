import { Players } from "@rbxts/services";

const Moderators = new Set<number>([]);
const Developers = new Set<number>([]);
const Owners = new Set<number>([]);

export default { Moderators, Owners, Developers };
